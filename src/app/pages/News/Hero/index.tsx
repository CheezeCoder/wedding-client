import * as React from 'react';
import * as Styles from './styles.css';

interface HeroProps {}

export const Hero: React.StatelessComponent<HeroProps> = () => (
  <div className={Styles.root}>{''}</div>
);
